/// Nombres de ruta centralizados (GetPage), para no repetir strings sueltos
/// por las pantallas.
class AppRutas {
  AppRutas._();

  static const inicio = '/';
  static const ajustes = '/ajustes';
  static const estadisticas = '/estadisticas';
  static const categorias = '/categorias';
}
