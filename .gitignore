# macOS
.DS_Store

# Claude Code local session settings (not the vendored skills, those are tracked)
.claude/settings.local.json

# backend (ASP.NET Core / EF Core)
backend/**/bin/
backend/**/obj/

# IDE (Rider/ReSharper, Visual Studio) — repo-wide
.idea/
.vs/
*.user
*.suo

# web (Angular)
web/node_modules/
web/dist/
web/.angular/

# shared local secrets
.env
